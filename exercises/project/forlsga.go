//Decision

// list of elevator id, position and direction
//Send list every time <-atFloor

func cost () {
    //calculate som cost based on the elevators in list
    //best elev = elev with lowest cost
    //return best elev
}

func decision() {
    //id = cost from function above
    //if elevID == id {
        set queue local
    }
    else { stay remote}
}